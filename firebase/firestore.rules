rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: open access. Production에서는 인증 기반 규칙으로 교체하세요.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
